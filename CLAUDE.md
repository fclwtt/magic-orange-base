## Agent skills

### Issue tracker

Issues 存放在 GitHub Issues，通过 `gh` CLI 操作。外部 PRs 不作为 triage 入口。详见 `docs/agents/issue-tracker.md`。

### Triage labels

五个分类标签使用默认名称：needs-triage、needs-info、ready-for-agent、ready-for-human、wontfix。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文（single-context）：根目录一个 CONTEXT.md + docs/adr/。详见 `docs/agents/domain.md`。
